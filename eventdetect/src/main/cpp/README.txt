use the build-all-thread-local.sh for building the real source code from this directory.

make sure you: export LD_LIBRARY_PATH=./

then run it with:

./bin/ed resources/841074643079602176.mp4 resources/mask1.png

if you have Code::Blocks installed as a C/C++ IDE, you can run it by $ codeblocks



